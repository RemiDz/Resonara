Initialise this directory as the Resonara project. Run npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/\*" and then build everything from CLAUDE.md. Set git remote to https://github.com/RemiDz/Resonara.git. Build the full audio processing library (audioContext, fftAnalyser, rt60, overtoneDetector, sweepGenerator, noiseFloor, transientDetector) in src/lib/audio/, the React hooks (useAudioStream, useFFT, useImpulseCapture) in src/hooks/, and the complete Room Discovery UI flow (DiscoveryStepper with OpeningScreen, AmbientListen, ClapTest, SpatialWalk placeholder, InstrumentProfiler placeholder) in src/components/discovery/ with MandalaCanvas visualisation, GlassPanel/GoldButton/ProgressRing/ScoreDisplay UI primitives, and cosmic dark theme. Commit and push to main when done.

